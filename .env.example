# Environment Configuration
# Copy file ini menjadi .env dan sesuaikan dengan konfigurasi server Anda
# PENTING: Jangan commit .env ke git!

# Application Settings
APP_ENV=production
APP_DEBUG=false
APP_URL=https://yourdomain.com

# Database Configuration - PostgreSQL
# Untuk hosting, ganti DB_HOST dengan IP/hostname server database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=toko_inventori
DB_USER=postgres
DB_PASS=your_secure_password_here

# Timezone
TIMEZONE=Asia/Jakarta

# Seeding control
# Production: set SEED_FORCE=true hanya saat inisialisasi awal, lalu kembalikan ke false
SEED_FORCE=false
# Password default akun awal (digunakan hanya jika user belum ada)
# Wajib diisi pada production saat first seed
SEED_ADMIN_PASSWORD=change_this_admin_password
SEED_KASIR_PASSWORD=change_this_kasir_password

# Notes:
# 1. APP_ENV: set ke 'production' untuk production, 'development' untuk dev
# 2. APP_DEBUG: HARUS false di production untuk security
# 3. DB_HOST: gunakan IP address jika database di server berbeda
# 4. DB_PASS: gunakan password yang kuat dan random
# 5. Restart application setelah mengubah .env
